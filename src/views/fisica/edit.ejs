<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Editar datos de Física</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>

  <header>
    <nav>
      <a href="/">🏠 Inicio</a>
      <a href="/fisica">📋 Ver datos</a>
    </nav>
  </header>

  <h1>Editar datos</h1>

  <% if (fisica) { %>
    <div class="container-center">
      <section class="contenedor form-panel">
        <form action="/fisica/<%= fisica.id_fisica %>?_method=PUT" method="POST">
          
          <label for="voltaje">Voltaje (V)</label>
          <input type="text" id="voltaje" name="voltaje" value="<%= fisica.voltaje %>" required>

          <label for="temperatura">Temperatura (°C)</label>
          <input type="text" id="temperatura" name="temperatura" value="<%= fisica.temperatura %>" required>

          <label for="distancia">Distancia del objeto (m)</label>
          <input type="text" id="distancia" name="distancia" value="<%= fisica.distancia %>" required>

          <button type="submit">Actualizar datos</button>
        </form>
      </section>

      <div style="margin-top:12px;">
        <a href="/fisica">⬅️ Volver a la lista</a>
      </div>
    </div>
  <% } else { %>
    <div class="container-center">
      <p style="color:red;">No se encontró el registro.</p>
      <a href="/fisica">⬅️ Volver a la lista</a>
    </div>
  <% } %>

</body>

</html>
