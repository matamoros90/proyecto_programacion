<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Ingresar datos de Física</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <header>
    <nav>
      <a href="/">🏠 Inicio</a>
      <a href="/fisica">📋 Ver datos</a>
    </nav>
  </header>

  <h1>Ingresar datos de Física</h1>

  <div class="container-center">
    <section class="contenedor form-panel">

      <% if (typeof mensaje !== 'undefined' && mensaje) { %>
        <div class="mensaje-exito">
          <p style="color: rgb(72, 255, 0);"><%= mensaje %></p>
        </div>
      <% } %>

      <form action="/fisica" method="POST">
        <label>Voltaje</label>
        <input type="text" name="voltaje" required>

        <label>Temperatura</label>
        <input type="text" name="temperatura" required>

        <label>Distancia del objeto</label>
        <input type="text" name="distancia" required>

        <label>Fecha (opcional)</label>
        <input type="datetime-local" name="fecha">

        <button type="submit">Guardar</button>
      </form>

    </section>
  </div>
</body>
</html>