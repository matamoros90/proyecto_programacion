<tbody>
  <% items.forEach(f => { %>
    <tr>
      <!-- ID -->
      <td><%= f.ID_FISICA %></td>

      <!-- tus demás celdas de datos -->
      <td><%= f.voltaje %></td>
      <td><%= f.temperatura %></td>
      <td><%= f.distancia %></td>
      <td>
        <% const raw = f.Fecha_Datos;
           const d = raw ? new Date(typeof raw === 'string' ? raw.replace(' ','T') : raw) : null; %>
        <%= d ? d.toISOString().replace('T',' ').slice(0,16) : '—' %>
      </td>

      <!-- Acciones -->
      <td>
        <a href="/fisica/<%= f.ID_FISICA %>">Ver</a>
        <a href="/fisica/<%= f.ID_FISICA %>/edit">Editar</a>
        <form action="/fisica/<%= f.ID_FISICA %>?_method=DELETE"
              method="POST" style="display:inline">
          <button type="submit"
                  onclick="return confirm('¿Seguro que desea eliminar?')">
            Eliminar
          </button>
        </form>
      </td>
    </tr>
  <% }) %>
</tbody>
