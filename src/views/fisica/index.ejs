<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Datos de física</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>

  <header>
    <nav>
      <a href="/">🏠 Inicio</a> 
      <a href="/fisica/create">➕ Ingresar datos nuevos</a>
    </nav>
  </header>

  <h1>Datos Generales del Levitador</h1>

  <% if (Array.isArray(fisica) && fisica.length > 0) { %>
    <div class="container-center">
      <section class="contenedor table-panel">
        <table>
          <thead>
            <tr>
              <th>Id</th>
              <th>Voltaje</th>
              <th>Temperatura</th>
              <th>Distancia</th>
              <th>Fecha</th>
              <th colspan="3">Acciones</th>
            </tr>
          </thead>
          <tbody>
            <% fisica.forEach(f => { %>
              <tr>
                <td><%= f.id_fisica %></td>
                <td><%= f.voltaje %></td>
                <td><%= f.temperatura %></td>
                <td><%= f.distancia %></td>
                <td>
                  <% 
                    const raw = f.Fecha_Datos;
                    const d = raw ? new Date(typeof raw === 'string' ? raw.replace(' ','T') : raw) : null;
                  %>
                  <%= d ? d.toISOString().replace('T',' ').slice(0,16) : '—' %>
                </td>
                <a href="/fisica/<%= r.id_fisica %>">Ver</a>
                <a href="/fisica/<%= r.id_fisica %>/edit">Editar</a>
                <form action="/fisica/<%= r.id_fisica %>?_method=DELETE" method="POST"> … </form>
                    <button type="submit" onclick="return confirm('¿Seguro que desea eliminar?')">Eliminar</button>
                  </form>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </section>
    </div>
  <% } else { %>
    <div class="container-center">
      <p>No hay registros aún. <a href="/fisica/create">Crea el primero</a>.</p>
    </div>
  <% } %>

</body>
</html>
